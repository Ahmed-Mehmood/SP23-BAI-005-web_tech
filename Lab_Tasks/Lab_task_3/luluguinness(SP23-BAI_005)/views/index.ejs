<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lulu Guinness</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        .text-pink {
            color: #ff69b4;
        }
        .btn-pink {
            background-color: #ff69b4;
            color: white;
        }
        .btn-pink:hover {
            background-color: #ff1493;
            color: white;
        }
    </style>
</head>
<body>

    <!-- <section class="hero position-relative text-center">
        <img src="pictures/lulu-guinness.jpg" class="w-100" alt="Hero Image">
        <div class="hero-text position-absolute top-50 start-50 translate-middle bg-dark bg-opacity-50 text-white p-4 rounded">
            <h1 class="fs-3">CHOOSE LOVE WITH LULU GUINNESS</h1>
            <p>Explore our latest collaboration with Laura Whitmore.</p>
            <a href="#" class="btn btn-pink">Discover Now</a>
        </div>
    </section> -->

    <section class="product-showcase py-5 text-center">
        <h2 class="mb-4">New Arrivals</h2>
        <div class="container">
            <div class="row g-4 justify-content-center">
                <% if (products && products.length > 0) { %>
                    <% products.forEach(product => { %>
                        <div class="col-md-3">
                            <div class="product p-3 border">
                                <img src="/media/images/<%= product.image %>" class="img-fluid" alt="<%= product.title %>">
                                <h3 class="fs-6 mt-2"><%= product.title %></h3>
                                <p class="text-pink fw-bold">£<%= product.price.toFixed(2) %></p>
                                <div class="mt-2">
                                    <a href="/product/<%= product._id %>" class="btn btn-outline-dark btn-sm">View Details</a>
                                    <form action="/cart/add/<%= product._id %>" method="post" class="d-inline">
                                        <button type="submit" class="btn btn-pink btn-sm">Add to Cart</button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    <% }); %>
                <% } else { %>
                    <div class="col-12">
                        <p>No products available at the moment.</p>
                    </div>
                <% } %>
            </div>
        </div>
    </section>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

