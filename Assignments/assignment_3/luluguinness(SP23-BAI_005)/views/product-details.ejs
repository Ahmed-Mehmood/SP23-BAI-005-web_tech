<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= product.title %> - Lulu Guinness</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .product-image {
            max-height: 500px;
            object-fit: contain;
        }
        .text-pink {
            color: #ff69b4;
        }
        .btn-pink {
            background-color: #ff69b4;
            color: white;
        }
        .btn-pink:hover {
            background-color: #ff1493;
            color: white;
        }
    </style>
</head>
<body>

    <div class="container py-5">
        <div class="row">
            <div class="col-md-6">
                <img src="/media/images/<%= product.image %>" alt="<%= product.title %>" class="img-fluid product-image">
            </div>
            <div class="col-md-6">
                <h1 class="h2 mb-3"><%= product.title %></h1>
                <p class="text-pink h3 mb-4">£<%= product.price.toFixed(2) %></p>
                <div class="mb-4">
                    <h3 class="h5">Description</h3>
                    <p><%= product.description %></p>
                </div>
                <div class="mb-4">
                    <h3 class="h5">Category</h3>
                    <p><%= product.category %></p>
                </div>
                <div class="mb-4">
                    <h3 class="h5">Stock</h3>
                    <p><%= product.stock %> units available</p>
                </div>
                <div class="d-grid gap-2">
                    <form action="/cart/add/<%= product._id %>" method="post">
                    <button class="btn btn-pink ">Add to Cart</button>
                    </form>
                    <a href="/" class="btn btn-outline-dark">Continue Shopping</a>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 